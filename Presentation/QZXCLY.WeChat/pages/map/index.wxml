<view class="container">
  <view class="map_section section">
    <map id="map" longitude="{{lon}}" latitude="{{lat}}" scale="{{level}}" controls="{{currentControls}}" bindmarkertap="bindmarkertap" markers="{{markers}}" polyline='{{polylines}}' bindcontroltap="controltap" show-location>
      <!-- <cover-view hidden='{{showSingelSpot}}' class='search_section top_section'>
        <cover-view class="search" bindtap="tapSearch">
          <cover-image class="icon" src="/resources/images/index/icon_search.png"/>
          <cover-view class="placeholder">搜索你想去的地方...</cover-view>
        </cover-view> 
      </cover-view>     -->

      <cover-view hidden='{{!showSingelSpot}}'  class='spot_section top_section'>
        <cover-view><cover-image class="avatar" src="{{currentSpot.logo}}" /></cover-view>
        <cover-view class='content'>
          <cover-view class='title'>{{currentSpot.title}}</cover-view>
          <cover-view class='info'>
            距离当前位置9.4km
          </cover-view>   
          <cover-view class='btns'>
            <cover-view data-url="../index/spot" class='navigator inline-block' bindtap='nav'>详情</cover-view>
            <cover-view class='navigator inline-block' bindtap='showImages'>图集</cover-view>     
            <cover-view data-url="nav"  data-lon="{{currentSpot.longitude}}" data-lat="{{currentSpot.latitude}}" class='navigator inline-block' bindtap='nav'>路线</cover-view>
          </cover-view>
        </cover-view>        
        <cover-image class='close' src="/resources/images/map/close.png" bindtap='returnToAll'></cover-image>

      </cover-view>
      <cover-view hidden='{{!showSingelSpot}}' class='spotFilter_section  absolute'>
        <cover-view class='filters'>
          <cover-view class="filter {{filterType=='service'?'active':''}}" data-id="service" bindtap="tapFilter">服务点</cover-view>     
          <cover-view class="filter {{filterType=='play'?'active':''}}}}" data-id="play" bindtap="tapFilter">景点</cover-view>
          <cover-view class="filter {{filterType=='eat'?'active':''}}" data-id="eat" bindtap="tapFilter">美食</cover-view>  
          <cover-view class="filter {{filterType=='live'?'active':''}}" data-id="live" bindtap="tapFilter">住宿</cover-view>
        </cover-view>
      </cover-view>     

       <cover-view class='drawer_screen' wx:if="{{showModalStatus}}"></cover-view> 
      <!--使用animation属性指定需要执行的动画  animation="{{animationData}}" -->  
      <cover-view  class="drawer_box"  wx:if="{{showModalStatus}}"> 
         <cover-view class="drawer_logo"><cover-image class="avatar" src="{{drawerMarker.logo}}" /></cover-view>  
        <!--drawer content-->  
        <cover-view class="drawer_content">               
          <cover-view class="drawer_title">{{drawerMarker.title}}</cover-view>
          <cover-view class='drawer_detail' wx:if="{{drawerMarker.desc}}">{{drawerMarker.desc}}</cover-view>
          <cover-view class='drawer_info' wx:if="{{filterType=='eat'?'active':''}}">星级：{{drawerMarker.level}}</cover-view>  
          <cover-view class='drawer_info' wx:if="{{filterType=='eat'?'active':''}}">人均消费：{{drawerMarker.price}} 元</cover-view>         
          <cover-view class='drawer_btns absolute'>  
            <cover-view data-url="{{drawerMarker.detailUrl}}" class='navigator inline-block' bindtap='nav'>详情</cover-view>
            <cover-view wx:if="{{drawerMarker.audioUr}}" data-audio="{{drawerMarker.audioUrl}}" class='navigator inline-block'>讲解</cover-view>     
            <cover-view wx:if="{{drawerMarker.phone}}" data-phone="{{drawerMarker.phone}}" class='navigator inline-block'>电话</cover-view>                 
            <cover-view wx:if="{{drawerMarker.panoramaUrl}}" data-url="../index/webview?src={{drawerMarker.panoramaUrl}}" class='navigator inline-block' bindtap='nav'>全景</cover-view>             
            <cover-view data-url="nav" data-lon="{{drawerMarker.lon}}" data-lat="{{drawerMarker.lat}}" class='navigator inline-block' bindtap='nav'>路线</cover-view>
          </cover-view>
        </cover-view>         
        <cover-view class="btn_ok" bindtap="powerDrawer" data-statu="close">关闭</cover-view> 
      </cover-view>      
    </map>     
  </view>    
</view>